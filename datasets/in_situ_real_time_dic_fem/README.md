# In-Situ Real-Time DIC-FEM ?????

??????????????????????????????????? DIC-FEM ???????
???????????????????? DIC ???FEM ????????????????

## ??????

- ???????? 7 200 s?????????????
- ????120 ?????? 1 Hz????? 60 s?
- ?????20 mm ? 20 mm????? 48 ? 48
- ?????2025???????
- DIC ???????? 1.5 ?m????????????
- FEM ??????????? + ????????????????????

## ????

- `metadata.json`???????????????
- `boundary_conditions/temperature_profile.csv`???????????
- `dic/real_time_dic_fields.npz`?DIC ???????/???
- `fem/fem_predictions.npz`?FEM ???????????????????
- `residuals/field_residuals.npz`?DIC ? FEM ???
- `residuals/residual_statistics.csv`???????
- `quicklook/dic_displacement_timeseries.csv`???????????
- `logs/digital_twin_log.json`??????????????

## ????

?? `.npz` ?????? `numpy.load(path)` ????????????

- `time_s`???????
- `temperature_c`???????????
- `x_coords` / `y_coords`????????mm?
- `ux`, `uy`, `uz`??????mm?
- `exx`, `eyy`, `exy`???????
- FEM ?????
  - `sxx`, `syy`, `sxy`??????MPa?
  - `svm`??? von Mises ???MPa?
  - `plastic_strain`???????
  - `damage`??????0~1?
  - `relative_density`???????
  - `densification`, `creep_activation`??????????????

## ??????

```python
from pathlib import Path
import numpy as np

dataset_dir = Path("datasets/in_situ_real_time_dic_fem")
dic = np.load(dataset_dir / "dic" / "real_time_dic_fields.npz")
fem = np.load(dataset_dir / "fem" / "fem_predictions.npz")
residual = np.load(dataset_dir / "residuals" / "field_residuals.npz")

print("????:", dic["time_s"].shape[0])
centre = (dic["ux"].shape[1] // 2, dic["ux"].shape[2] // 2)
print("??? DIC Ux (mm):", dic["ux"][:, centre[0], centre[1]])
print("von Mises ??? (MPa):", fem["svm"].max())
print("Ux ????? (mm):", np.sqrt((residual["ux"] ** 2).mean()))
```

## ????

- ??????/?????????? `scripts/generate_in_situ_dic_fem_dataset.py` ??? `--time-steps` ? `--grid-size`
- ?? `field_residuals.npz` ???????Kalman/EnKF?????????????
- `digital_twin_log.json` ?????????????????????????

## ???

???????????????????????

> ?A Closed-Loop, Microstructure-Informed DIC-FEM Framework for the Real-Time Mitigation of Sintering Stresses in Solid Oxide Fuel Cells through Targeted Creep Activation????????
