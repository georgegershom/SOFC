# SOFC?????????

## ?? ?????

????????SOFC??????????!

**?????**: 7.2 MB  
**????**: 24????? + 8??????  
**???**: ??(NiO-YSZ)????(8YSZ)???(LSM-YSZ)????(Crofer22APU)

---

## ?? ????

### 1. ??????????

```bash
ls -R sofc_dataset/
```

### 2. ???????

??????: `sofc_dataset/visualization/`

????:
- `sintering_kinetics.png` - ???????(6???)
- `cte_analysis.png` - ???????
- `anode/electrolyte/cathode_creep_behavior.png` - ??????
- `norton_parameters.png` - Norton??????
- `elastic_properties.png` - ????
- `density_evolution.png` - ????

### 3. Python??????

#### ????????
```python
import json
import pandas as pd

# JSON??
with open('sofc_dataset/thermal_physical/green_state_properties.json', 'r') as f:
    green_props = json.load(f)
    
print("????:", green_props['anode']['density_green'], "g/cm?")
print("?????:", green_props['anode']['porosity_green'], "%")
```

#### ??CTE??
```python
import pandas as pd
import matplotlib.pyplot as plt

# ????CTE??
df_anode = pd.read_csv('sofc_dataset/thermal_physical/anode_CTE.csv')

# ??
plt.plot(df_anode['temperature_C'], df_anode['CTE_ppm_per_K'])
plt.xlabel('Temperature (?C)')
plt.ylabel('CTE (ppm/K)')
plt.title('Anode CTE')
plt.show()
```

#### ??Norton????
```python
import json

with open('sofc_dataset/constitutive_parameters/norton_law_parameters.json', 'r') as f:
    norton = json.load(f)

# ?????????
anode_params = norton['anode']
A = anode_params['A_pre_exponential']
n = anode_params['n_stress_exponent']
Q = anode_params['Q_activation_energy']

print(f"??Norton??:")
print(f"  A = {A:.2e} 1/(MPa^n?s)")
print(f"  n = {n:.2f}")
print(f"  Q = {Q:.1f} kJ/mol")

# ????????????
import numpy as np
T = 1200 + 273.15  # K
sigma = 2.0  # MPa
R = 8.314  # J/(mol?K)

creep_rate = A * (sigma**n) * np.exp(-Q*1000/(R*T))
print(f"\n?{T-273.15}?C, {sigma} MPa??????: {creep_rate:.2e} 1/s")
```

#### ????????
```python
import json

with open('sofc_dataset/creep_data/anode_creep_tests.json', 'r') as f:
    creep_tests = json.load(f)

# ??????: 1200?C, 2 MPa
test = creep_tests['T1200C_S2.0MPa']

time = test['time_hours']
strain = test['creep_strain']
rate = test['creep_rate_per_hour']
steady_rate = test['steady_state_rate']

print(f"????: {test['temperature_C']}?C, {test['stress_MPa']} MPa")
print(f"??????: {steady_rate:.2e} 1/h")
```

#### ?????????
```python
import pandas as pd

# ????
df = pd.read_csv('sofc_dataset/sintering_kinetics/anode_dilatometry.csv')
print("???????:", df.columns.tolist())

# ????
df_bilayer = pd.read_csv('sofc_dataset/sintering_kinetics/anode_electrolyte_bilayer.csv')
print("\n??CTE???:", df_bilayer['cte_mismatch_ppm_per_K'].max(), "ppm/K")
print("??????:", df_bilayer['estimated_stress_MPa'].max(), "MPa")
```

#### ????????
```python
import pandas as pd

df = pd.read_csv('sofc_dataset/elastic_properties/anode_elastic_properties.csv')

# ??????
# ???, 1000?C
dense_1000C = df[(df['relative_density'] > 0.95) & 
                  (abs(df['temperature_C'] - 1000) < 20)]

if len(dense_1000C) > 0:
    E = dense_1000C['youngs_modulus_GPa'].mean()
    nu = dense_1000C['poisson_ratio'].mean()
    print(f"???1000?C?????:")
    print(f"  ????: {E:.1f} GPa")
    print(f"  ???: {nu:.3f}")
```

---

## ?? ??????

### ????? (`thermal_physical/`)
- `green_state_properties.json/csv` - ??????
- `{layer}_CTE.csv` - ??????? (4???)

### ????? (`sintering_kinetics/`)
- `{layer}_dilatometry.csv` - ?????? (3???)
- `{layer1}_{layer2}_bilayer.csv` - ?????? (2???)
- `{layer}_density_evolution.csv` - ???? (3???)

### ???? (`creep_data/`)
- `{layer}_creep_tests.json` - ?????? (3???)
  - ??????42????? (7?? ? 6??)

### ???? (`constitutive_parameters/`)
- `norton_law_parameters.json/csv` - Norton????

### ???? (`elastic_properties/`)
- `{layer}_elastic_properties.csv` - ????? (4???)
  - ???????200???? (?? ? ??)

---

## ?? ??????

### 1. FEM??????

**ABAQUS??????? (UMAT)**:
```fortran
! ?Norton?????????
A = 1.5E-5  ! ?norton_law_parameters.json??
n = 2.2
Q = 450.0E3  ! J/mol
R = 8.314
T = TEMP + 273.15

creep_rate = A * (stress**n) * exp(-Q/(R*T))
```

**ANSYS????**:
- ??CTE????????????
- ????????????????????
- ??Norton????????

### 2. ???????

```python
# ??????
df = pd.read_csv('sofc_dataset/sintering_kinetics/anode_electrolyte_bilayer.csv')

# ??????
max_stress_temp = df.loc[df['estimated_stress_MPa'].idxmax(), 'temperature_C']
max_stress = df['estimated_stress_MPa'].max()

print(f"???????: {max_stress_temp:.0f}?C")
print(f"?????: {max_stress:.1f} MPa")

# ????????
# ???????????...
```

### 3. ??????

```python
# ??????
df = pd.read_csv('sofc_dataset/sintering_kinetics/anode_density_evolution.csv')

# ??95%?????????
idx_95 = df[df['relative_density'] >= 0.95].index[0]
time_95 = df.loc[idx_95, 'time_minutes']
temp_95 = df.loc[idx_95, 'temperature_C']

print(f"??95%?????:")
print(f"  ??: {time_95:.0f} ??")
print(f"  ??: {temp_95:.0f}?C")
```

### 4. ????????

```python
import numpy as np
import json

with open('sofc_dataset/constitutive_parameters/norton_law_parameters.json', 'r') as f:
    norton = json.load(f)

# ??????????????
temps = np.array([1000, 1100, 1200, 1300, 1400])
stress = 2.0  # MPa

for layer in ['anode', 'electrolyte', 'cathode']:
    params = norton[layer]
    A = params['A_pre_exponential']
    n = params['n_stress_exponent']
    Q = params['Q_activation_energy']
    
    rates = A * (stress**n) * np.exp(-Q*1000/(8.314*(temps+273.15)))
    
    print(f"\n{layer.capitalize()} ???? (2 MPa):")
    for T, rate in zip(temps, rates):
        print(f"  {T}?C: {rate:.2e} 1/s")
```

---

## ?? ??????

??????,????????:

### 1. ???????
```python
# ??CTE???
df_cte = pd.read_csv('sofc_dataset/thermal_physical/anode_CTE.csv')
print("CTE??:", df_cte['CTE_ppm_per_K'].min(), "-", df_cte['CTE_ppm_per_K'].max())
# ???10-13 ppm/K??

# ??????
df_sint = pd.read_csv('sofc_dataset/sintering_kinetics/anode_dilatometry.csv')
print("????:", df_sint['linear_shrinkage_percent'].max(), "%")
# ???15-25%??
```

### 2. ?????
- YSZ?CTE: ~10.5 ppm/K (25-1000?C)
- NiO-YSZ?CTE: ~12.5 ppm/K
- LSM?CTE: ~11.5 ppm/K
- ????: 15-25%
- Norton n?: ??1.5-3.0

### 3. ???????
```python
# ??Norton???????????
# ??Norton????,???????
```

---

## ?? ????

???????????: **`DATASET_README.md`**

??:
- ?????????
- ??????
- ???????
- ????
- ?????

---

## ??? ????

### ???????
```bash
python3 generate_sofc_dataset.py
```

### ???????
```bash
python3 visualize_dataset.py
```

### ????
```bash
pip3 install -r requirements.txt
```

---

## ?? ????

1. **????**: ??????????????,?????????
2. **????**: ??????,??????????????
3. **????**: ??????????????????
4. **????**: `raw_experimental/`????????????

---

## ?? ????

### ???????

?? `generate_sofc_dataset.py` ????:
- `_get_green_density()` - ??????
- `_fit_norton_parameters()` - ??Norton??
- `_calculate_cte()` - ??CTE???
- ??...

### ???????

? `__init__()` ?? `self.materials` ????????:
```python
self.materials = {
    'anode': 'NiO-YSZ',
    'electrolyte': '8YSZ',
    'cathode': 'LSM-YSZ',
    'interconnect': 'Crofer22APU',
    'new_layer': 'YourMaterial'  # ????
}
```

???????????????

---

## ?? ?????

????????????????,??:
1. ???????????
2. ?????????
3. ?????????

---

**??????,??????SOFC???! ??**
